fwd_dataset:
  name: bootstrap_dataset
  class: model_zoo.utils.dataset.Dataset
  params:
    holdout_ratio: 0.2
    n_bootstraps: 7  # TODO set dynamically to fwd_model.num_components
    bootstrap_size: 1. # TODO rename this param